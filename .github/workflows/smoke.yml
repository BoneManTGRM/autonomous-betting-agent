name: Smoke

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CI: 'true'
      PYTHONUNBUFFERED: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pytest

      - name: Compile
        run: |
          python -m compileall -q streamlit_app.py app_streamlit.py autonomous_betting_agent pages tests scripts

      - name: Smoke pytest
        run: |
          set -o pipefail
          python -m pytest -q tests --disable-warnings --import-mode=importlib --tb=short 2>&1 | tee smoke-pytest.log

      - name: Upload smoke pytest log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-pytest-log
          path: smoke-pytest.log

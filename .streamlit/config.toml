# deploy trigger: sidebar-stale-deploy-check-20260618
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Hide Streamlit's automatic file-list sidebar. Each page renders the shared ABA sidebar.
showSidebarNavigation = false
toolbarMode = "minimal"

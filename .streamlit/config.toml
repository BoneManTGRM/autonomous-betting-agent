# deploy trigger: hide-native-nav-keep-custom-sidebar-v1
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Native Streamlit file discovery lists every page in /pages. Keep it off.
showSidebarNavigation = false
toolbarMode = "minimal"

# deploy trigger: emergency-stable-native-nav-v6
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Emergency testing mode: keep Streamlit's native sidebar page list visible so navigation cannot disappear.
showSidebarNavigation = true
toolbarMode = "minimal"

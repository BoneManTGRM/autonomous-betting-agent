# deploy trigger: custom-bottom-sidebar-links-v3
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Keep native Streamlit page list hidden. Pages are rendered below language selector by sitecustomize/page links.
showSidebarNavigation = false
toolbarMode = "minimal"

# deploy trigger: enable-native-nav-signal-board-v2
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Required for st.navigation(..., position='sidebar') to render the page list.
showSidebarNavigation = true
toolbarMode = "minimal"

# deploy trigger: single-sidebar-no-duplicates-v11
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Hide Streamlit's automatic file-list sidebar. Each page renders the shared ABA sidebar.
showSidebarNavigation = false
toolbarMode = "minimal"

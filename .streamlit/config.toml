# deploy trigger: legacy-sidebar-visible-v14
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Hide Streamlit's automatic file-list sidebar. Each page renders the shared ABA sidebar or legacy fallback tools.
showSidebarNavigation = false
toolbarMode = "minimal"

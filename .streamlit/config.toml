# deploy trigger: clean-custom-tools-sidebar-v5
[server]
headless = true

[browser]
gatherUsageStats = false

[client]
# Hide Streamlit's automatic pages/ file list. Use the app's custom Tools menu instead.
showSidebarNavigation = false
toolbarMode = "minimal"
